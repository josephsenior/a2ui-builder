# Local env example for A2UI Builder
# For local MCP server (dev):
MCP_SERVER_URL=http://localhost:7423
MCP_API_KEY=
GITHUB_PERSONAL_ACCESS_TOKEN=

# LLM / model keys (if used):
NEXT_PUBLIC_GEMINI_API_KEY=

# Optional monitoring
SENTRY_DSN=

# Usage:
# 1. Copy this file to .env.local
# 2. Fill in your secrets (never commit .env.local to git)
# 3. Start the MCP server with: npm run dev:mcp:sse
#    This loads the vars from .env.local for a persistent local dev experience.
